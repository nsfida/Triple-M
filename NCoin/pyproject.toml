[project]
name = "ncoin"
version = "0.1.0"
description = "NCoin decentralized cryptocurrency node implementation"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
ncoin = "ncoin.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
